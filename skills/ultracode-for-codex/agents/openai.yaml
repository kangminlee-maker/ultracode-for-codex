interface:
  display_name: "Ultracode for Codex"
  short_description: "Run durable, resumable Codex workflows."
  default_prompt: "Use the Codex main context for decisions and edits, and use the Ultracode CLI runtime for schema-enforced, journaled, resumable fan-out phases with visible progress."
